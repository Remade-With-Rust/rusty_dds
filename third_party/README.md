# Official Microsoft DDS peer for decode A/B (DirectXTex).
# Clone: git clone --depth 1 https://github.com/microsoft/DirectXTex.git
# This tree is not vendored in git by default (large); see tools/dxtex_decode_bench.
DirectXTex/
