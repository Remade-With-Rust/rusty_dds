# third_party/ (not published)

Optional Microsoft DirectXTex tree for local A/B only:

```text
git clone --depth 1 https://github.com/microsoft/DirectXTex.git DirectXTex
```

Not vendored in git (large). Not required to use or build `rusty_dds`.
